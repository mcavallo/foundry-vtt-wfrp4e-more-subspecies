<form class='flexcol'>
  <div>
    <div class='warning'><i class='fas fa-exclamation-triangle'></i>&nbsp;&nbsp;<strong
      >Warning:</strong>
      Changing the available datasets during character generation can lead to issues.
      Please make sure to
      <u>always do it beforehand</u>.</div>
    <p>Select which datasets should be available.</p>
    <div class='item-list'>
      {{#each datasets}}
        <label class='item'>
          <input
            type='checkbox'
            value='{{id}}'
            data-dtype='String'
            name='datasets'
            {{checked enabled}}
          />
          <div class='item-details'>
            <div class='label'>
              {{{label}}}
            </div>
            <div class='description'>
              {{{description}}}
            </div>
          </div>
        </label>
      {{/each}}
    </div>
  </div>
  <footer class='sheet-footer flexrow'>
    <button type='submit' name='submit'>
      <i class='fa fa-save'></i>
      Save Enabled Datasets
    </button>
  </footer>
</form>